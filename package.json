{
    "private": true,
    "scripts": {
        "clean": "rimraf output && rimraf .pulp-cache",
        "build": "eslint src && spago build"
    },
    "devDependencies": {
        "eslint": "^8.15.0",
        "pulp": "^16.0.1",
        "purescript-psa": "^0.8.2",
        "rimraf": "^3.0.2",
        "spago": "^0.20.9"
    }
}
